---
title: Button
description: DOE 界面的基础操作组件 / UI Action Component。
---

import { ButtonDemo } from "@/components/docs/demos/button-demo"

## 预览 / Preview

<ButtonDemo />

## 用法 / Usage

```tsx
import { Button } from "@/components/ui/button"

export function Example() {
  return <Button>发布 Round</Button>
}
```

`Button` 用于普通 UI actions。领域含义应该由页面文案和 Domain Components 提供，而不是重新实现一个新的按钮组件。
