---
title: Button
description: The base UI action component for DOE interfaces.
---

## Usage

```tsx
import { Button } from "@/components/ui/button"

export function Example() {
  return <Button>Release Round</Button>
}
```

Use `Button` for ordinary UI actions. Domain-specific meaning should come from surrounding copy and domain components, not from a new button implementation.
